# 造神计划手机独立版 PRD

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 产品名称 | 造神计划 |
| 产品版本 | 手机独立版 V1.0 |
| 产品形态 | 可安装的 PWA 手机应用 |
| 核心目标 | 用户无需电脑开机，即可在手机上随时使用造神计划 |
| 首发用户 | 产品创建者本人 |
| 后续用户 | 少量受邀测试用户 |

## 2. 产品背景

当前版本需要电脑运行本地服务，手机与电脑连接同一 Wi-Fi 后才能访问。该方式适合开发测试，但无法满足日常长期使用需求。

手机独立版需要部署到公网 HTTPS 地址，使用户能够通过手机浏览器打开，并安装到手机桌面。用户安装后可像普通 App 一样启动，在无网络时仍能完成打卡和复盘。

## 3. 产品目标

### 3.1 核心目标

- 不依赖电脑开机和局域网。
- 用户通过固定网址访问产品。
- 支持安装到手机桌面。
- 支持离线打开、打卡和填写复盘。
- 网络恢复后，应用可继续正常使用。
- 为后续账号登录、云同步和换机恢复预留能力。

### 3.2 成功标准

- 手机首次打开时间不超过 3 秒。
- 用户能够成功将应用添加到手机桌面。
- 安装后可以通过桌面图标独立启动。
- 断网后仍可打开应用并完成核心操作。
- 应用更新后，用户无需重新安装。
- 用户数据不会因普通版本更新而丢失。

## 4. 用户场景

### 场景一：首次使用

用户通过手机打开产品网址，创建自己的六周计划，并将应用添加到手机桌面。

### 场景二：日常打卡

用户点击手机桌面上的“造神计划”图标，查看今日任务并完成打卡，无需打开电脑。

### 场景三：离线使用

用户在无网络环境下打开应用，继续打卡和填写复盘。数据先保存在手机本地。

### 场景四：版本更新

产品发布新版本后，用户重新打开应用即可获得更新，同时保留已有计划和记录。

### 场景五：换手机或清理数据

V1.0 中，用户需要通过数据导出和导入恢复记录。后续版本支持账号云同步。

## 5. 版本范围

### 5.1 V1.0 必须包含

- 公网 HTTPS 固定访问地址
- 手机桌面安装能力
- 独立窗口启动
- 离线加载核心页面
- 离线完成计划创建、打卡和复盘
- 本地数据长期保存
- 应用版本更新机制
- 数据导出为备份文件
- 从备份文件导入数据
- 清除数据二次确认
- 安装与备份使用说明

### 5.2 V1.0 不包含

- 微信登录
- 手机号或邮箱登录
- 自动云同步
- 多设备实时同步
- 应用商店上架
- 消息推送
- 社交与分享功能

### 5.3 V1.1 计划包含

- 用户账号
- 自动云端备份
- 换机恢复
- 多设备数据同步
- 同步失败提示与冲突处理

## 6. 核心功能需求

### 6.1 公网访问与部署

#### 功能说明

产品部署到支持 HTTPS 的静态网站托管平台，并获得长期稳定的网址。

#### 要求

- 所有页面、脚本、样式、图标和离线资源均通过 HTTPS 加载。
- 访问任意应用路径时，可以正常返回应用首页。
- 发布新版本时，不改变用户访问网址。
- 部署失败时，旧版本继续可用。

#### 验收标准

- 手机使用移动网络时可以访问。
- 手机关闭与电脑的 Wi-Fi 连接后仍可访问。
- 浏览器地址栏显示安全连接。

### 6.2 安装到手机桌面

#### 功能说明

用户可将造神计划安装到手机桌面，通过独立窗口打开。

#### 要求

- 应用名称显示为“造神计划”。
- 使用独立应用图标。
- 启动时不显示普通浏览器地址栏。
- 页面适配刘海屏、圆角屏和底部安全区域。
- 应用内提供简短安装引导。

#### 验收标准

- 安卓 Chrome 支持“安装应用”。
- iPhone Safari 支持“添加到主屏幕”。
- 从桌面启动后，核心页面显示正常。

### 6.3 离线使用

#### 功能说明

首次联网打开后，用户可以在无网络状态下继续使用核心功能。

#### 离线可用功能

- 打开应用
- 查看当前计划
- 查看今日任务
- 完成或取消打卡
- 填写和编辑复盘
- 查看本机历史统计
- 编辑计划

#### 离线不可用功能

- 检查和下载新版本
- 未来的账号登录与云同步

#### 验收标准

- 首次联网加载完成后，开启飞行模式仍可打开应用。
- 离线打卡和复盘不会丢失。
- 网络恢复后，本地数据保持一致。

### 6.4 本地数据保存

#### 功能说明

V1.0 的计划、打卡和复盘数据保存在当前手机浏览器中。

#### 要求

- 数据保存使用统一版本结构。
- 应用更新不清除用户数据。
- 数据读取失败时，显示可理解的错误提示。
- 清除数据必须二次确认。
- 明确提示用户：卸载应用、清理浏览器数据或更换手机可能导致记录丢失。

### 6.5 数据导出与导入

#### 功能说明

用户可手动备份数据，并在当前手机或新手机恢复。

#### 数据导出

- 在“我的”页面提供“导出备份”入口。
- 导出内容包含当前计划、历史计划、打卡和复盘。
- 文件名称格式：`造神计划备份-YYYY-MM-DD.json`。
- 导出前提示备份文件包含个人记录，请妥善保存。

#### 数据导入

- 在“我的”页面提供“导入备份”入口。
- 只接受符合造神计划数据格式的 JSON 文件。
- 导入前展示备份日期和记录概要。
- 导入会覆盖当前数据，必须二次确认。
- 无效文件不得覆盖当前数据。

#### 验收标准

- 导出文件可成功下载或保存到手机文件应用。
- 清除数据后，使用备份文件可以完整恢复。
- 导入损坏文件时，当前数据保持不变。

### 6.6 应用更新

#### 功能说明

新版本发布后，应用自动获取更新，同时保护用户数据。

#### 要求

- 静态资源使用版本化缓存。
- 新版本准备完成后提示用户刷新。
- 更新失败时继续使用旧版本。
- 更新过程不得删除本地数据。

#### 验收标准

- 发布新版本后，重新打开应用能够获得更新。
- 更新后计划、打卡和复盘仍然存在。
- 离线时仍可使用最近成功加载的版本。

### 6.7 安装与数据安全提示

#### 页面内容

“我的”页面增加“手机使用说明”：

- 如何添加到手机桌面
- 如何开启离线使用
- 数据保存在哪里
- 如何导出备份
- 换手机时如何恢复
- 哪些操作可能导致数据丢失

## 7. 页面调整

### 今日页

- 保持现有任务、进度和打卡功能。
- 无网络时显示轻量“离线使用中”状态，不影响操作。

### 我的页

新增以下入口：

- 安装到手机
- 导出数据备份
- 导入数据备份
- 手机使用说明
- 当前应用版本
- 检查更新

### 更新提示

- 新版本可用时，底部显示非阻塞提示。
- 用户点击“立即更新”后刷新应用。

## 8. 数据结构与兼容

继续使用统一的 `godPlan:v1` 本地数据结构：

- `activePlan`
- `checkins`
- `reviews`
- `archivedPlans`

导出文件增加以下元数据：

```json
{
  "product": "god-plan",
  "exportVersion": 1,
  "exportedAt": "ISO 日期时间",
  "data": {}
}
```

导入时必须验证：

- `product` 必须为 `god-plan`
- `exportVersion` 必须为支持的版本
- `data` 必须包含有效的数据结构
- 验证通过前不得修改当前数据

## 9. 非功能需求

- 支持 iOS Safari 和安卓 Chrome 的近期版本。
- 支持 320px 以上手机屏幕。
- 所有公网资源必须使用 HTTPS。
- 首屏资源尽量控制在 1MB 以内。
- 无网络时不得出现空白页面。
- 用户数据仅保存在本机，除非用户主动导出。
- 备份文件不得包含代码、执行脚本或外部链接。

## 10. 核心流程

### 首次安装

1. 用户通过手机打开固定 HTTPS 地址。
2. 用户创建六周计划。
3. 产品引导用户添加到手机桌面。
4. 用户从桌面图标重新打开应用。

### 日常使用

1. 用户从手机桌面打开应用。
2. 查看并完成今日任务。
3. 睡前填写复盘。
4. 数据自动保存在手机本地。

### 备份与换机

1. 用户在旧手机导出备份文件。
2. 用户在新手机打开并安装造神计划。
3. 用户选择导入备份。
4. 系统验证文件并展示概要。
5. 用户二次确认后恢复数据。

## 11. 实施顺序

### 第一阶段：真正脱离电脑

- 将当前 PWA 部署到公网 HTTPS 地址。
- 验证手机访问、安装和独立启动。
- 验证飞行模式下的核心功能。

### 第二阶段：保护用户数据

- 增加数据导出和导入。
- 增加备份说明与数据丢失提醒。
- 验证换机恢复流程。

### 第三阶段：完善更新体验

- 优化离线缓存策略。
- 增加版本号和更新提示。
- 验证更新不会破坏已有数据。

## 12. 最终验收清单

- 电脑关闭后，手机仍可正常访问应用。
- 使用移动网络可以正常访问应用。
- 手机可以将应用添加到主屏幕。
- 从主屏幕启动时显示为独立应用。
- 开启飞行模式后可以打开、打卡和复盘。
- 关闭并重新打开后，本地记录仍存在。
- 可以导出完整备份。
- 可以通过备份恢复全部数据。
- 无效备份不会覆盖当前数据。
- 发布新版本后，用户数据不会丢失。
- iPhone Safari 与安卓 Chrome 均完成实机验证。

