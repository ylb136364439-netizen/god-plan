# 造神计划：项目设计与开发全流程学习手册

> 本文完整记录“造神计划”从一张参考图、产品需求、原型开发，到手机独立安装、离线使用、数据备份、自动测试和 GitHub Pages 上线的全过程。  
> 它既是项目复盘文档，也是一份可以照着练习的前端 PWA 实战教程。

---

## 目录

1. [项目最终成果](#1-项目最终成果)
2. [从参考图提炼产品，而不是照抄界面](#2-从参考图提炼产品而不是照抄界面)
3. [如何编写和收敛 PRD](#3-如何编写和收敛-prd)
4. [技术选型与架构决策](#4-技术选型与架构决策)
5. [项目目录与各文件职责](#5-项目目录与各文件职责)
6. [数据模型设计](#6-数据模型设计)
7. [日期、周次和任务生成算法](#7-日期周次和任务生成算法)
8. [本地存储、迁移和版本兼容](#8-本地存储迁移和版本兼容)
9. [页面渲染与交互设计](#9-页面渲染与交互设计)
10. [统计功能的实现方法](#10-统计功能的实现方法)
11. [手机响应式 UI 设计](#11-手机响应式-ui-设计)
12. [PWA 安装能力](#12-pwa-安装能力)
13. [Service Worker 离线与更新机制](#13-service-worker-离线与更新机制)
14. [数据备份、验证和恢复](#14-数据备份验证和恢复)
15. [测试策略和自动检查](#15-测试策略和自动检查)
16. [无依赖构建系统](#16-无依赖构建系统)
17. [Git、GitHub Actions 和 Pages 上线](#17-gitgithub-actions-和-pages-上线)
18. [本次开发遇到的问题与排查方法](#18-本次开发遇到的问题与排查方法)
19. [安全、隐私和当前限制](#19-安全隐私和当前限制)
20. [可以继续升级的方向](#20-可以继续升级的方向)
21. [建议的学习与练习路线](#21-建议的学习与练习路线)
22. [常用命令速查](#22-常用命令速查)

---

## 1. 项目最终成果

### 1.1 产品定位

“造神计划”是一款六周个人成长计划 PWA。用户可以创建六周计划，每天完成阅读、俯卧撑、游泳和睡前复盘等任务，并查看执行记录。

### 1.2 正式地址

- 应用地址：<https://ylb136364439-netizen.github.io/god-plan/>
- GitHub 仓库：<https://github.com/ylb136364439-netizen/god-plan>

### 1.3 已实现能力

- 创建和编辑固定六周计划
- 根据日期和星期自动生成今日任务
- 每日任务打卡与取消打卡
- 今日完成百分比与连续完成天数
- 六周路线和当前阶段状态
- 每日复盘自动保存
- 填写任意复盘内容后自动完成复盘任务
- 月度打卡日历与各目标完成率
- 手机桌面安装
- 离线打开、打卡和复盘
- 在线与离线状态提示
- 数据备份导出、验证与恢复
- 新版本更新提示
- GitHub Actions 自动测试与发布

### 1.4 当前边界

- 没有账号系统
- 没有云端数据库
- 数据保存在当前浏览器的 `localStorage`
- 换手机前需要手动导出备份
- 暂无消息推送和自动云同步

这是一种非常典型的 MVP 取舍：先做完整可用的核心闭环，再决定是否投入后端和账号系统。

---

## 2. 从参考图提炼产品，而不是照抄界面

项目最初只有一张横向计划表参考图。图中包含四类目标：

- 读书
- 俯卧撑
- 游泳
- 睡前复盘

以及按周逐步增加强度的路线。

### 2.1 参考图表达了什么

参考图的核心价值不是它的排版，而是它传达的产品模型：

```text
长期目标
  ↓
按周拆分阶段
  ↓
形成每日行动
  ↓
通过复盘持续调整
```

### 2.2 为什么不能直接照抄横向表格

横向表格适合电脑或图片展示，但不适合手机：

- 手机宽度有限
- 横向滚动会让用户失去上下文
- 每天真正需要关注的是“今天做什么”
- 大量信息同时显示会增加行动压力

因此手机端采用了：

- 首页：只展示今日任务
- 计划页：纵向展示六周路线
- 复盘页：独立记录每日感受
- 我的页：展示历史统计和数据管理

### 2.3 可复用的方法

拿到参考图时，可以先回答以下问题：

1. 图中真正的数据实体是什么？
2. 用户最频繁的动作是什么？
3. 哪些内容每天看，哪些内容偶尔看？
4. 原图的布局是否适合目标设备？
5. 哪些信息是装饰，哪些信息决定产品逻辑？

这个过程叫做从“视觉参考”提取“产品结构”。

---

## 3. 如何编写和收敛 PRD

项目先后形成了两份 PRD：

- `PRD.md`：核心六周成长计划
- `手机独立版PRD.md`：手机安装、离线和数据备份

### 3.1 第一份 PRD 解决的问题

第一份 PRD 明确：

- 产品为谁服务
- 用户每天为什么打开
- 六周计划如何运作
- MVP 包含哪些页面
- 哪些功能第一版明确不做

### 3.2 第二份 PRD 为什么必要

最初版本虽然能在手机浏览器访问，但依赖电脑运行本地服务。用户进一步提出“单独在手机上用”，这改变了产品要求：

- 必须有公网 HTTPS 地址
- 必须支持添加到手机桌面
- 必须支持离线
- 必须考虑浏览器数据丢失
- 必须提供备份恢复
- 必须提供安全更新机制

这说明需求不是静态的。产品开发中，用户的新目标可能要求新增一份专项 PRD，而不是简单加一个按钮。

### 3.3 PRD 中最重要的内容

一个可以真正指导开发的 PRD 至少要包含：

#### 产品目标

例如：

> 用户无需电脑开机，即可在手机上随时使用造神计划。

#### 成功标准

例如：

- 断网后仍能打开应用
- 更新后数据不会丢失
- 手机能够添加到主屏幕

#### 功能范围

明确“做什么”和“不做什么”。不做的内容同样重要，因为它防止范围无限膨胀。

#### 验收标准

验收标准应该可以被测试，而不是使用模糊描述。

不够明确：

> 应用应该很好用。

可以验收：

> 在 320px、375px、430px 手机宽度下无横向滚动。

### 3.4 决策收敛

实施前锁定了以下关键决策：

- 首发形态：PWA
- 第一批用户：个人使用
- 同时运行的计划数：一个
- 周期：固定六周
- 存储方式：本机 `localStorage`
- 托管平台：GitHub Pages
- 仓库：公开仓库
- 发布方式：推送后自动部署

这些决策一旦确定，工程实现就不需要不断停下来重新选择。

---

## 4. 技术选型与架构决策

### 4.1 为什么选择 PWA

PWA 是 Progressive Web App，即渐进式 Web 应用。

它同时具备网页与 App 的一部分特征：

- 通过网址访问
- 可添加到手机桌面
- 可以独立窗口打开
- 可以离线使用
- 更新不需要应用商店审核

对于个人 MVP，它比原生 App 和微信小程序更合适：

| 方案 | 优点 | 缺点 |
| --- | --- | --- |
| 原生 App | 系统能力强 | 开发和发布成本高 |
| 微信小程序 | 微信内使用方便 | 需要小程序账号、审核与平台适配 |
| PWA | 开发快、部署简单、跨平台 | 系统推送和后台能力受限制 |

### 4.2 为什么使用原生 HTML/CSS/JavaScript

本项目没有引入 React、Vue 或第三方依赖，原因是：

- 页面数量少
- 状态模型简单
- 目标是快速可用
- 无依赖更容易离线和部署
- 学习时可以直接理解浏览器原生能力

这不是说框架不好，而是技术选型应该匹配项目规模。

### 4.3 架构分层

项目主要分成三层：

```text
界面层
index.html + styles.css + app.js

领域与数据层
store.js

平台与交付层
manifest.webmanifest + sw.js + scripts + GitHub Actions
```

#### 界面层

负责：

- 显示页面
- 监听点击与输入
- 调用数据层方法
- 重新渲染

#### 数据层

负责：

- 创建计划
- 日期计算
- 任务生成
- 打卡保存
- 统计计算
- 备份导入导出

#### 平台层

负责：

- 安装到桌面
- 离线缓存
- 应用更新
- 测试、构建和部署

分层的价值是让业务逻辑不依赖页面 DOM，从而更容易测试。

---

## 5. 项目目录与各文件职责

```text
造神计划/
├─ index.html                  页面结构
├─ styles.css                 所有视觉与响应式样式
├─ app-config.js              应用版本号
├─ app.js                     页面渲染、用户交互和 PWA 交互
├─ store.js                   数据、日期、任务、统计和备份逻辑
├─ sw.js                      Service Worker 离线与更新
├─ manifest.webmanifest       PWA 安装信息
├─ icon.svg                   矢量图标源
├─ icon-192.png               安卓常用图标
├─ icon-512.png               PWA 大图标和 maskable 图标
├─ apple-touch-icon.png       iPhone 桌面图标
├─ dev-server.js              无依赖本地静态服务器
├─ store.test.js              核心数据规则测试
├─ package.json               npm 命令入口
├─ scripts/
│  ├─ generate-icons.js       生成 PNG 应用图标
│  ├─ verify.js               检查资源、引用、Manifest 和语法
│  └─ build.js                生成正式发布目录
├─ .github/workflows/pages.yml 自动测试与 GitHub Pages 发布
├─ PRD.md                     核心产品需求
├─ 手机独立版PRD.md            手机独立版需求
└─ README.md                  使用说明
```

### 5.1 为什么 `store.js` 和 `app.js` 分开

错误的做法是把所有逻辑都写在点击事件中：

```js
button.onclick = () => {
  // 读取数据
  // 算日期
  // 保存数据
  // 更新统计
  // 修改页面
};
```

这样很快会变得难以测试和维护。

本项目改成：

```js
GP.toggleCheckin(date, taskId, store);
store = GP.loadStore();
renderAll();
```

页面只负责调用，具体规则放在数据层。

---

## 6. 数据模型设计

### 6.1 顶层存储结构

所有数据统一保存在一个 `localStorage` 键中：

```js
const STORAGE_KEY = "godPlan:v1";
```

数据结构：

```json
{
  "version": 1,
  "activePlan": {},
  "checkins": {},
  "reviews": {},
  "archivedPlans": []
}
```

### 6.2 为什么使用一个统一键

优点：

- 读取和保存简单
- 备份时可以一次导出
- 数据版本容易管理
- 避免不同键之间出现状态不一致

缺点：

- 数据量非常大时，每次保存整个对象效率较低

本项目数据量很小，因此统一对象是合理选择。

### 6.3 当前计划 `activePlan`

示意结构：

```json
{
  "id": "plan-时间戳",
  "name": "造神计划",
  "startDate": "2026-06-09",
  "endDate": "2026-07-20",
  "status": "active",
  "goals": {},
  "weeks": [],
  "createdAt": "ISO 时间"
}
```

### 6.4 每日打卡 `checkins`

按日期存储：

```json
{
  "2026-06-09": {
    "read": true,
    "pushup": true,
    "review": false
  }
}
```

使用日期作为键有几个好处：

- 查询某一天非常直接
- 生成月历简单
- 计算连续天数简单
- 备份结构清晰

### 6.5 每日复盘 `reviews`

```json
{
  "2026-06-09": {
    "work": "完成了核心功能",
    "sport": "状态不错",
    "growth": "学会了 PWA",
    "money": "无额外开支",
    "tomorrow": "验证离线功能",
    "updatedAt": "ISO 时间"
  }
}
```

### 6.6 历史计划 `archivedPlans`

重新开始新计划时，旧计划会归档，并带上当时的打卡和复盘：

```js
store.archivedPlans.push({
  ...store.activePlan,
  archivedAt: new Date().toISOString(),
  checkins: clone(store.checkins),
  reviews: clone(store.reviews)
});
```

这里必须深拷贝，否则后续清空或修改当前数据时，历史数据也可能被引用影响。

---

## 7. 日期、周次和任务生成算法

日期处理是这个项目中最重要、也最容易出错的部分。

### 7.1 为什么使用 `YYYY-MM-DD`

项目统一使用：

```text
2026-06-09
```

优点：

- 字符串天然可排序
- 适合做对象键
- 不依赖语言显示格式
- 与 HTML `input[type=date]` 兼容

### 7.2 避免时区导致日期偏移

直接使用：

```js
new Date("2026-06-09")
```

浏览器可能将其按 UTC 解析，在某些时区出现日期偏移。

本项目显式拆分年月日，并使用本地中午：

```js
function parseDateKey(key) {
  const [year, month, day] = key.split("-").map(Number);
  return new Date(year, month - 1, day, 12);
}
```

为什么使用 12 点而不是 0 点？

因为本地午夜更容易在时区转换或夏令时边界上出现偏移。中午是更稳妥的日期锚点。

### 7.3 计算两个日期相差几天

```js
function daysBetween(start, end) {
  const asUtc = key => {
    const [year, month, day] = key.split("-").map(Number);
    return Date.UTC(year, month - 1, day);
  };
  return Math.round((asUtc(end) - asUtc(start)) / 86400000);
}
```

这里用 UTC 仅用于计算纯日期差，避免本地时区和夏令时导致一天不等于固定毫秒数。

### 7.4 计划状态计算

六周等于 42 天，偏移范围是 `0` 到 `41`：

```js
function getPlanDay(date, plan) {
  const offset = daysBetween(plan.startDate, date);

  if (offset < 0) return { state: "upcoming" };
  if (offset > 41) return { state: "ended" };

  return {
    state: "active",
    day: offset + 1,
    week: Math.floor(offset / 7) + 1
  };
}
```

这是典型的“状态派生”：

- 不需要每天保存当前是第几周
- 只需要保存开始日期
- 每次根据日期重新计算

派生数据通常比重复保存更可靠。

### 7.5 动态生成任务

每日固定任务：

- 阅读
- 俯卧撑
- 睡前复盘

游泳只在设定星期出现：

```js
if (plan.goals.swim.weekdays.includes(parseDateKey(date).getDay())) {
  tasks.splice(2, 0, {
    id: "swim",
    ...plan.goals.swim,
    detail: week.swim
  });
}
```

这里没有把 42 天的所有任务提前存入数据库，而是在需要时动态生成。

优点：

- 数据更少
- 修改未来计划时无需批量更新任务
- 业务规则集中在一个函数中

---

## 8. 本地存储、迁移和版本兼容

### 8.1 `localStorage` 的特点

`localStorage`：

- 以键值字符串保存数据
- 同一个网站来源下长期保留
- 同步 API，使用简单
- 不适合存大量数据
- 用户清理浏览器数据后会消失

保存对象时必须序列化：

```js
localStorage.setItem(STORAGE_KEY, JSON.stringify(store));
```

读取时反序列化：

```js
const saved = JSON.parse(localStorage.getItem(STORAGE_KEY));
```

### 8.2 数据版本

顶层保存：

```json
{ "version": 1 }
```

未来改变数据结构时，可以增加：

```text
godPlan:v2
```

或者根据 `version` 执行迁移。

### 8.3 旧数据迁移

早期原型按日期分别保存：

```text
god-plan-habits-日期
god-plan-review-日期
```

最终版本会在第一次加载时扫描这些旧键，并迁移到统一结构：

```js
function migrateLegacy(store) {
  Object.keys(localStorage)
    .filter(key => key.startsWith("god-plan-habits-") || key.startsWith("god-plan-review-"))
    .forEach(/* 转换 */);
}
```

这个知识点叫做向后兼容或数据迁移。产品升级不能简单假设用户没有旧数据。

### 8.4 容错读取

读取存储时使用 `try/catch`：

```js
try {
  const saved = JSON.parse(localStorage.getItem(STORAGE_KEY));
  if (saved?.version === 1) return saved;
} catch (_) {}
```

原因：

- 本地数据可能损坏
- 用户可能手动修改
- 旧版本可能保存了无效 JSON

数据读取失败时，应用至少应能回到可用状态，而不是整个页面崩溃。

---

## 9. 页面渲染与交互设计

### 9.1 单页应用结构

项目没有使用路由框架，而是同时放置四个页面区域：

```html
<section id="todayPage"></section>
<section id="planPage"></section>
<section id="reviewPage"></section>
<section id="profilePage"></section>
```

通过 CSS 类切换显示：

```js
function openPage(pageId) {
  $$(".page, .nav-item").forEach(item => item.classList.remove("active"));
  $(`#${pageId}`).classList.add("active");
  $(`.nav-item[data-page="${pageId}"]`).classList.add("active");
}
```

这是一个轻量级 SPA 思路。

### 9.2 渲染函数

项目将界面拆成：

```js
renderToday();
renderPlan();
renderReview();
renderProfile();
```

总入口：

```js
function renderAll() {
  renderToday();
  renderPlan();
  renderReview();
  renderProfile();
}
```

每次数据修改后：

1. 保存数据
2. 重新读取数据
3. 重新渲染

这是一个简单但清晰的单向数据流：

```text
用户动作 → 修改 store → 保存 → render → 新界面
```

### 9.3 为什么需要 HTML 转义

用户可以编辑计划名称和目标名称。如果直接把用户输入拼入 `innerHTML`，可能造成 XSS：

```js
const escapeHtml = value =>
  String(value).replace(/[&<>"']/g, char => mapping[char]);
```

凡是把用户输入放入 HTML 字符串，都应该转义。

### 9.4 弹窗设计

项目使用原生 `<dialog>`：

```js
$("#planDialog").showModal();
$("#planDialog").close();
```

优点：

- 浏览器原生遮罩
- 自动管理焦点
- 语义更明确
- 不需要自己实现弹层定位

### 9.5 自动保存复盘

复盘表单监听 `input`：

```js
$("#reviewForm").oninput = () => {
  GP.saveReview(reviewDate, review, store);
};
```

用户每次输入都会保存，因此不需要单独点击“保存”。

同时，填写任意字段后自动完成复盘任务：

```js
store.checkins[date].review =
  Object.values(review).some(value => String(value).trim());
```

这是产品规则与交互细节结合的例子：降低用户完成复盘的心理门槛。

---

## 10. 统计功能的实现方法

### 10.1 每日完成比例

```js
function completionForDate(date, store) {
  const tasks = getTasksForDate(date, store.activePlan);
  const done = tasks.filter(task => store.checkins[date]?.[task.id]).length;
  return {
    done,
    total: tasks.length,
    ratio: done / tasks.length
  };
}
```

### 10.2 各目标完成率

遍历计划开始日至今天：

```text
每一天
  → 生成当天应做任务
  → 每个任务总次数 +1
  → 已完成则完成次数 +1
```

最后：

```js
完成率 = 已完成次数 / 应完成次数 × 100
```

重要点：游泳不是每天都有，因此不能简单用“完成天数 / 总天数”，必须基于实际生成的任务数。

### 10.3 连续完成天数

本项目把“当天全部任务完成”视为完整打卡日。

特殊处理：

```js
if (end === today && completionForDate(end, store)?.ratio !== 1) {
  streakEnd = addDays(end, -1);
}
```

为什么？

如果今天刚开始，用户还没完成任务，不应该立刻把昨天的连续记录显示为 0。连续天数从昨天开始回溯，更符合用户感受。

### 10.4 月历

月历需要：

1. 当前月第一天是星期几
2. 当前月有多少天
3. 每一天的完成比例

```js
const first = new Date(year, month, 1).getDay();
const days = new Date(year, month + 1, 0).getDate();
```

显示状态：

- 未执行：无底色
- 部分完成：暖色
- 全部完成：深绿色

---

## 11. 手机响应式 UI 设计

### 11.1 视觉方向

项目采用：

- 暖米白背景
- 深绿色主色
- 柔和阴影
- 大圆角卡片
- 不使用红色惩罚未完成任务

目标是“温暖、有力量，但不制造焦虑”。

### 11.2 设计变量

CSS 变量集中定义视觉系统：

```css
:root {
  --ink: #17201b;
  --muted: #667069;
  --paper: #f5f2e9;
  --card: #fffdf7;
  --green: #285943;
  --green-soft: #dcebe1;
}
```

优点：

- 改主题时不需要全文搜索颜色
- 视觉语义清晰
- 保证一致性

### 11.3 手机安全区域

底部导航使用：

```css
bottom: max(12px, env(safe-area-inset-bottom));
```

`safe-area-inset-bottom` 用于避开 iPhone 底部手势区域。

页面 `viewport` 中使用：

```html
<meta name="viewport"
  content="width=device-width, initial-scale=1.0, viewport-fit=cover">
```

`viewport-fit=cover` 允许页面延伸到刘海屏和圆角区域，再通过安全区域变量避让。

### 11.4 响应式布局

项目主要以手机为默认布局，较宽屏幕再增强：

```css
@media (min-width: 700px) {
  .habit-list {
    grid-template-columns: 1fr 1fr;
  }
}
```

这叫移动优先设计。

### 11.5 真实发现的横向滚动问题

最初 CSS 写了：

```css
body {
  min-width: 320px;
}
```

在自动化浏览器中，320px 视口出现竖向滚动条后，实际内容宽度只剩约 305px，`min-width: 320px` 反而造成横向滚动。

修复为：

```css
body {
  min-width: 0;
}
```

然后重新验证：

- 320px：无横向滚动
- 375px：无横向滚动
- 430px：无横向滚动

经验：不要只凭感觉写响应式 CSS，要在真实目标宽度检查 `scrollWidth` 与 `clientWidth`。

---

## 12. PWA 安装能力

PWA 可安装至少需要：

- HTTPS
- Web App Manifest
- Service Worker
- 合适的图标

### 12.1 Manifest

`manifest.webmanifest` 告诉浏览器应用如何安装：

```json
{
  "name": "造神计划",
  "short_name": "造神计划",
  "id": "./",
  "start_url": "./",
  "scope": "./",
  "display": "standalone",
  "orientation": "portrait-primary"
}
```

#### `display: standalone`

安装后以独立应用窗口运行，不显示普通浏览器地址栏。

#### `start_url` 和 `scope`

项目部署在：

```text
/god-plan/
```

使用相对路径 `./`，可以同时兼容：

- 本地根路径
- GitHub Pages 子路径

这是部署到 GitHub Pages 时非常重要的细节。

### 12.2 图标

项目提供：

- `icon-192.png`
- `icon-512.png`
- `apple-touch-icon.png`

图标由 `scripts/generate-icons.js` 无依赖生成。

脚本手动构造 PNG 数据：

- 生成 RGBA 像素
- 使用 zlib 压缩
- 计算 PNG chunk CRC
- 写出 PNG 文件

这个脚本不是生产项目的唯一选择，但展示了二进制文件格式、像素绘制和自动化资源生成的思路。

### 12.3 安卓安装事件

浏览器可能触发：

```js
window.addEventListener("beforeinstallprompt", event => {
  event.preventDefault();
  installPrompt = event;
});
```

用户点击安装按钮时：

```js
installPrompt.prompt();
await installPrompt.userChoice;
```

### 12.4 iPhone 安装

iOS Safari 通常不提供可编程安装弹窗，因此应用展示操作说明：

1. Safari 打开
2. 点击分享
3. 选择“添加到主屏幕”
4. 点击添加

这说明跨平台产品不能假设所有平台拥有相同 API。

---

## 13. Service Worker 离线与更新机制

Service Worker 是运行在页面之外的浏览器脚本，可以拦截网络请求并使用缓存。

### 13.1 应用外壳缓存

```js
const APP_SHELL = [
  "./",
  "index.html",
  "styles.css",
  "app-config.js",
  "store.js",
  "app.js",
  "manifest.webmanifest",
  "icon-192.png"
];
```

安装阶段预缓存：

```js
self.addEventListener("install", event => {
  event.waitUntil(
    caches.open(CACHE).then(cache => cache.addAll(APP_SHELL))
  );
});
```

这保证首次加载完成后，离线时仍有页面、样式和脚本。

### 13.2 导航请求策略

```js
if (event.request.mode === "navigate") {
  event.respondWith(
    fetch(event.request).catch(() => caches.match("./"))
  );
}
```

含义：

- 有网络：优先请求最新页面
- 无网络：回退到缓存首页

### 13.3 静态资源策略

```js
caches.match(event.request)
  .then(cached => cached || fetch(event.request))
```

即缓存优先：

- 速度快
- 离线可靠
- 新版本通过更换缓存名更新

### 13.4 缓存版本号

```js
const CACHE = "god-plan-v5";
```

发布新版本时修改缓存名。激活新 Service Worker 时删除旧缓存：

```js
caches.keys().then(keys =>
  Promise.all(
    keys.filter(key => key !== CACHE)
      .map(key => caches.delete(key))
  )
);
```

### 13.5 为什么不在安装时立即 `skipWaiting`

如果新版本安装完成后立即接管，可能导致用户正在填写复盘时页面被刷新。

最终方案：

1. 新 Service Worker 安装并等待
2. 页面显示“发现新版本”
3. 用户点击“立即更新”
4. 页面发送消息：

```js
waitingWorker.postMessage({ type: "SKIP_WAITING" });
```

5. Service Worker 执行：

```js
self.skipWaiting();
```

6. 控制器变化后刷新：

```js
navigator.serviceWorker.addEventListener(
  "controllerchange",
  () => window.location.reload()
);
```

这是更尊重用户当前操作的更新策略。

### 13.6 首次安装误提示更新问题

正式环境验证时发现，首次访问也可能出现更新提示。

修复：

```js
if (registration.waiting && navigator.serviceWorker.controller) {
  showUpdate(registration.waiting);
}
```

只有已经存在控制当前页面的旧 Service Worker 时，等待中的 Worker 才是真正的新版本。

### 13.7 在线与离线状态

```js
window.addEventListener("online", updateConnectionStatus);
window.addEventListener("offline", updateConnectionStatus);
```

需要理解：

`navigator.onLine` 只能说明设备是否连接网络，不保证目标服务器一定可访问。因此它适合作为轻量提示，不适合做严格联网判断。

---

## 14. 数据备份、验证和恢复

由于 V1.0 没有云同步，备份恢复是保护用户数据的关键能力。

### 14.1 备份格式

```json
{
  "product": "god-plan",
  "exportVersion": 1,
  "exportedAt": "ISO 日期时间",
  "data": {
    "version": 1,
    "activePlan": {},
    "checkins": {},
    "reviews": {},
    "archivedPlans": []
  }
}
```

### 14.2 为什么需要外层元数据

不能直接导出 store，因为导入时需要判断：

- 这是哪个产品生成的文件？
- 备份格式版本是什么？
- 什么时候导出的？

因此外层加入：

- `product`
- `exportVersion`
- `exportedAt`

### 14.3 导出

```js
function exportBackup(store = loadStore()) {
  return {
    product: PRODUCT_ID,
    exportVersion: EXPORT_VERSION,
    exportedAt: new Date().toISOString(),
    data: clone(store)
  };
}
```

### 14.4 手机分享与下载回退

优先使用 Web Share API：

```js
if (navigator.canShare?.({ files: [file] })) {
  navigator.share({ files: [file] });
}
```

不支持时创建临时下载链接：

```js
const link = document.createElement("a");
link.href = URL.createObjectURL(blob);
link.download = filename;
link.click();
```

这是一种渐进增强：

- 支持高级能力的平台获得更好体验
- 不支持的平台仍有基本功能

### 14.5 导入前验证

绝不能读取 JSON 后直接覆盖数据。

验证顺序：

1. 文件是否为对象
2. `product` 是否正确
3. `exportVersion` 是否支持
4. `data.version` 是否正确
5. `checkins`、`reviews` 是否为对象
6. `archivedPlans` 是否为数组
7. 当前计划结构是否合理

```js
function importBackup(backup) {
  const validation = validateBackup(backup);
  if (!validation.valid) throw new Error(validation.error);
  return saveStore(clone(backup.data));
}
```

只有验证通过，才允许写入。

### 14.6 为什么需要二次确认

导入会覆盖当前数据，清除数据不可恢复，这些属于高风险操作。

因此产品使用二次确认，降低误操作风险。

---

## 15. 测试策略和自动检查

项目没有测试框架依赖，而是使用 Node 内置能力测试。

### 15.1 数据层测试

`store.test.js` 使用：

- `assert`
- `vm`
- 模拟 `localStorage`

因为 `store.js` 原本运行在浏览器中，Node 没有 `window` 和 `localStorage`，所以创建测试上下文：

```js
const context = {
  window: {},
  localStorage,
  Date,
  console
};

vm.createContext(context);
vm.runInContext(fs.readFileSync("store.js", "utf8"), context);
```

这使浏览器脚本可以在 Node 中执行。

### 15.2 已测试的核心规则

- 第一天属于第 1 周
- 第八天属于第 2 周
- 42 天后计划结束
- 周二生成游泳任务
- 周三不生成游泳任务
- 填写复盘自动完成复盘打卡
- 连续天数计算
- 旧计划归档
- 备份格式验证
- 无效备份被拒绝
- 清空后可以从备份恢复

### 15.3 静态资源检查

`scripts/verify.js` 检查：

- 必须文件是否存在
- `app.js` 引用的页面 ID 是否真的存在
- Manifest 是否可解析
- 是否包含 512px 图标
- JavaScript 是否有语法错误

页面引用检查示意：

```js
const ids = new Set(
  [...html.matchAll(/id="([^"]+)"/g)].map(match => match[1])
);

const refs = [
  ...app.matchAll(/\$\("#([^" ]+)"\)/g)
].map(match => match[1]);
```

这类检查可以在发布前发现“JS 查找了不存在的元素”。

### 15.4 浏览器真实验证

自动测试之外，还使用真实浏览器验证：

- 创建计划弹窗
- 我的页面手机管理入口
- 使用说明弹窗
- 320px、375px、430px 宽度
- 控制台错误
- 正式 GitHub Pages 环境
- 版本更新后提示是否消失

测试的层次应该是：

```text
纯函数规则测试
  +
静态资源检查
  +
真实浏览器交互测试
  +
正式环境验证
```

---

## 16. 无依赖构建系统

### 16.1 npm 命令

`package.json`：

```json
{
  "scripts": {
    "icons": "node scripts/generate-icons.js",
    "start": "node dev-server.js",
    "test": "node store.test.js && node scripts/verify.js",
    "build": "node scripts/build.js"
  }
}
```

即使项目没有第三方包，`npm scripts` 仍然适合统一命令入口。

### 16.2 为什么需要构建目录

源码目录里还有：

- PRD
- 测试代码
- 学习文档
- 本地服务器

这些不应该全部发布到网站。

`scripts/build.js` 只复制正式应用资源到 `dist`：

```js
const assets = [
  "index.html",
  "styles.css",
  "app-config.js",
  "store.js",
  "app.js",
  "sw.js",
  "manifest.webmanifest",
  "icon-192.png",
  "icon-512.png",
  "apple-touch-icon.png"
];
```

原则：发布产物应该是明确白名单，而不是把整个项目目录上传。

### 16.3 `.nojekyll`

GitHub Pages 默认可能使用 Jekyll 处理网站。构建脚本写入：

```text
.nojekyll
```

告诉 GitHub Pages 直接作为静态文件发布。

---

## 17. Git、GitHub Actions 和 Pages 上线

### 17.1 Git 初始化

```powershell
git init -b main
git add .
git commit -m "Release mobile standalone PWA v1.0"
```

Git 的价值：

- 保存每次变更历史
- 支持回退
- 支持远程协作
- GitHub Actions 以提交为部署触发点

### 17.2 GitHub 仓库

仓库选择公开，因为 GitHub Free 的 Pages 使用公开仓库最直接。

代码公开不等于用户数据公开：

- GitHub 仓库只有应用代码
- 用户计划和复盘保存在手机浏览器中
- `localStorage` 不会被提交到 Git

### 17.3 GitHub Actions 工作流

文件：

```text
.github/workflows/pages.yml
```

触发条件：

```yaml
on:
  push:
    branches: ["main"]
  workflow_dispatch:
```

含义：

- 推送到 `main` 自动运行
- 也可以手动触发

### 17.4 工作流分为两个 Job

#### Build

```yaml
- uses: actions/checkout@v6
- uses: actions/setup-node@v6
- run: npm test
- run: npm run build
- uses: actions/upload-pages-artifact@v4
```

步骤：

1. 下载代码
2. 准备 Node
3. 运行测试
4. 构建 `dist`
5. 上传发布产物

#### Deploy

```yaml
- uses: actions/deploy-pages@v4
```

只有 Build 成功后才发布。

这叫持续集成与持续部署，简称 CI/CD。

### 17.5 GitHub Pages 为什么适合本项目

- 免费 HTTPS
- 固定网址
- 静态文件托管
- 与 GitHub Actions 集成
- 每次推送自动更新
- 电脑关闭后手机仍可访问

### 17.6 首次部署失败的原因

第一次工作流在 Pages 启用前已经开始运行，因此失败。

处理方法：

1. 使用 GitHub API 启用 Pages
2. 重新触发工作流
3. 等待工作流完成
4. 检查正式网址

工程中很多失败不是代码错误，而是资源创建与执行时序问题。

---

## 18. 本次开发遇到的问题与排查方法

### 18.1 PowerShell 中文显示乱码

问题表现：

- 默认读取中文文件时终端显示乱码

排查方法：

```powershell
Get-Content -Raw -Encoding utf8 文件名
```

结论：

- 文件本身是 UTF-8
- 问题只是终端默认编码读取方式

经验：看到乱码不要立即重写文件，先确认是显示问题还是文件内容真的损坏。

### 18.2 大补丁新增文件失败

问题：

- 一次补丁同时删除并新增一个尚不存在的文件，工具无法读取旧文件

解决：

- 将大改动拆分
- 先单独新增文件
- 再修改已有文件

经验：大批量变更失败时，拆成可验证的小步骤。

### 18.3 Python 本地服务不可用

问题：

- 本机找不到 Python

解决：

- 编写无依赖 `dev-server.js`
- 直接使用已存在的 Node

额外价值：

- 项目启动方式统一
- 不依赖额外安装
- 可打印电脑与手机访问地址

### 18.4 本地服务器绑定地址

最初仅绑定：

```text
127.0.0.1
```

这只能让电脑自己访问。

改为：

```text
0.0.0.0
```

手机和电脑连接同一 Wi-Fi 后，可以通过电脑局域网 IP 访问。

知识点：

- `127.0.0.1`：仅本机
- `0.0.0.0`：监听所有网络接口

### 18.5 Git 仓库所有权安全提示

Git 检测到工作目录所有者与当前用户不一致，提示 `dubious ownership`。

解决：

```powershell
git config --global --add safe.directory "E:/Desktop/codex/造神计划"
```

这个机制用于防止在不可信目录中执行恶意 Git 配置。

### 18.6 GitHub CLI 登录等待

后台运行登录命令时，用户无法看到设备验证码，因此登录未完成。

解决：

- 安装官方 GitHub CLI
- 打开可见 PowerShell 窗口
- 用户完成一次浏览器授权

经验：需要人工交互的授权步骤必须提供可见反馈。

### 18.7 GitHub TLS 传输失败

推送时曾遇到 TLS 握手中断：

```text
SSL/TLS connection failed
unexpected eof while reading
```

排查步骤：

1. 检查 GitHub 443 端口
2. 检查 GitHub API 是否可用
3. 重试 Git 推送
4. 使用 GitHub API 提交紧急小修
5. 网络恢复后同步本地与远端提交历史

最终：

- 远端成功发布
- 本地和远端重新合并
- `main` 与 `origin/main` 同步

经验：Git 传输失败不代表 GitHub 整体不可用。需要区分：

- Git HTTPS 连接
- GitHub REST API
- GitHub Pages CDN
- GitHub Actions

### 18.8 正式环境版本变量异常

浏览器自动化读取正式环境时，发现 `window.APP_VERSION` 表现异常。

为增强容错，应用使用：

```js
const APP_VERSION = window.APP_VERSION || "1.0.0";
```

原则：配置脚本可能因缓存、加载顺序或网络问题暂时不可用，关键展示信息应有合理默认值。

### 18.9 不应只验证本地环境

本地测试通过并不代表正式环境一定正常。

最终正式验证包括：

- 正式 HTTPS 地址返回 200
- 所有 PWA 资源可访问
- GitHub Actions 成功
- 页面版本号正确
- 更新提示状态正确
- 正式环境控制台无错误

---

## 19. 安全、隐私和当前限制

### 19.1 用户数据在哪里

数据存储在：

```text
当前设备 + 当前浏览器 + 当前网站来源
```

不会自动发送到 GitHub。

### 19.2 什么会导致数据丢失

- 清理浏览器网站数据
- 卸载应用并清除数据
- 更换手机
- 使用另一个浏览器打开
- 浏览器隐私策略自动清理数据

所以增加了备份导出。

### 19.3 备份文件的隐私

备份包含：

- 计划
- 打卡记录
- 复盘文字

它可能包含个人隐私，应妥善保存。

### 19.4 导入安全

导入 JSON 不会执行其中内容，只会读取并验证数据结构。

仍需注意：

- 不信任外部文件
- 验证产品标识和版本
- 验证结构后才写入
- 覆盖前二次确认

### 19.5 XSS 防护

用户输入放入 HTML 前使用 `escapeHtml`。如果未来增加富文本，需要使用更严格的 HTML 清理库，而不能简单允许任意 HTML。

---

## 20. 可以继续升级的方向

### 20.1 云同步

引入后端后可以支持：

- 用户登录
- 自动云端备份
- 多设备同步
- 换机自动恢复

可能的技术方案：

- Supabase
- Firebase
- Cloudflare D1 + Workers
- 自建 Node API

### 20.2 IndexedDB

当数据量增长时，可以从 `localStorage` 迁移到 IndexedDB。

IndexedDB 优点：

- 容量更大
- 支持结构化数据
- 异步操作
- 更适合离线应用

### 20.3 通知提醒

可以研究：

- Web Push
- Notifications API
- 微信订阅消息
- 原生 App 本地通知

但 iOS 和 PWA 的通知权限及后台行为需要单独验证。

### 20.4 更完整的测试

可以加入：

- Playwright 自动端到端测试
- Service Worker 离线模拟测试
- Lighthouse PWA 检查
- 无障碍测试
- 备份大数据量测试

### 20.5 前端模块化

当前使用全局对象：

```js
window.GodPlanStore
```

未来可迁移为 ES Modules：

```js
export function loadStore() {}
import { loadStore } from "./store.js";
```

模块化可以改善依赖关系和测试体验。

### 20.6 更丰富的计划模型

可以支持：

- 多个并行计划
- 自定义周期
- 自定义目标类型
- 暂停与顺延
- 周报和总结
- AI 自动生成计划

但每增加一项，都要重新考虑数据结构和交互复杂度。

---

## 21. 建议的学习与练习路线

### 第一阶段：理解现有项目

1. 阅读 `PRD.md`
2. 阅读 `index.html`，理解页面结构
3. 阅读 `store.js`，画出数据结构
4. 阅读 `app.js`，找出每个按钮对应的事件
5. 阅读 `sw.js`，理解缓存流程

### 第二阶段：修改小功能

建议练习：

1. 修改主色
2. 修改默认六周计划内容
3. 增加一个复盘字段
4. 在“我的”页面增加总打卡次数
5. 给全部完成增加不同提示语

### 第三阶段：修改业务规则

建议练习：

1. 将计划周期从 6 周改为 8 周
2. 增加“跑步”目标
3. 设置阅读只在工作日出现
4. 连续天数改为“完成 80% 也算”
5. 增加暂停计划

修改后要同步：

- 数据模型
- 任务生成函数
- 统计函数
- UI
- 测试

### 第四阶段：学习工程化

1. 为新规则补测试
2. 运行 `npm test`
3. 运行 `npm run build`
4. 提交 Git
5. 推送后观察 GitHub Actions
6. 在正式网址验证

### 第五阶段：增加云同步

开始学习：

- HTTP API
- 用户认证
- 数据库
- 冲突解决
- 隐私和权限

---

## 22. 常用命令速查

### 本地运行

```powershell
cd E:\Desktop\codex\造神计划
npm start
```

访问：

```text
http://127.0.0.1:4173
```

### 运行测试

```powershell
npm test
```

### 生成图标

```powershell
npm run icons
```

### 构建正式发布目录

```powershell
npm run build
```

### 查看 Git 状态

```powershell
git status
```

### 保存一次修改

```powershell
git add .
git commit -m "描述本次修改"
git push
```

推送到 `main` 后，GitHub Actions 会自动测试并发布。

### 查看最近提交

```powershell
git log --oneline -5
```

### 查看自动部署

```powershell
gh run list --repo ylb136364439-netizen/god-plan
```

---

## 总结

这个项目的核心学习价值不只是“写了一个打卡页面”，而是完整经历了一个小型产品的全生命周期：

```text
参考图
→ 产品抽象
→ PRD
→ MVP 范围
→ 技术选型
→ 数据建模
→ 业务算法
→ 手机 UI
→ PWA 离线与安装
→ 数据安全
→ 自动测试
→ 构建
→ CI/CD
→ 正式上线
→ 正式环境排错
```

真正可靠的软件开发，不只是把功能写出来，还包括：

- 明确范围
- 设计数据
- 处理边界
- 保护用户数据
- 自动验证
- 安全部署
- 在正式环境中再次确认

建议阅读本文时，同时打开对应源码逐段对照。每理解一个章节，就尝试修改一个小功能并补充测试，这会比只阅读更容易真正掌握。

